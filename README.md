# ClaimsOps
Application-delivery / software-engineering portfolio project.

Run: open `index.html` in a browser.

Current local build:
- claim queue search/filtering
- severity, exposure, SLA-risk metrics
- lifecycle state transitions
- local persistence
- synthetic data generation

Production direction:
C#/.NET REST API, PostgreSQL, authentication/roles, validation, audit history, automated tests.
